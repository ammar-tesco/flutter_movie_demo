class MovieDomain{

  String id;
  String title;
  String imageUrl;
  String genres;
  String runtimeStr;
  String plot;
  String releaseState;
  String year;

  MovieDomain(this.id,this.title, this.imageUrl, this.genres, this.runtimeStr,
      this.plot, this.releaseState, this.year);
}